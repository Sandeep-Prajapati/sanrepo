pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                sh "date"
                sh "echo $HOME"
                sh "echo $USER"
                echo 'Hello world! ...' 
                sh "pwd"
            }
        }
    }
}